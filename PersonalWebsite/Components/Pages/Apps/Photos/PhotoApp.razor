@page "/apps/photos"

<AppWindow AppName="Photos">
   <div class="photos">
      @foreach (var photo in _photos)
      {
         <div class="image">
            <img alt="" src="@photo" />
         </div>
      }
   </div>
</AppWindow>